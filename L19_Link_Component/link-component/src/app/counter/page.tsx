export const metadata = {
    title: "Arbaz Ansari Counter",
    description: "counter"
}

import Counter from './counter'

function CounterApp() {
    return (
        <Counter />
    )
}

export default CounterApp